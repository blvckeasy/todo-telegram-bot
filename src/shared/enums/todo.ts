export enum TodoPriority {
    LOW = "low",
    MEDIUM = "medium",
    HIGH = "high",
}

export enum TodoStatus {
    NOT_COMPLATED = "not_complated",
    COMPLATED = "complated",
    DELETED = "deleted",
}